## Installations :
Flask==3.0.0
Flask-PyMongo==2.3.0
pymongo==4.6.1
Werkzeug==3.0.1
python-dotenv==1.0.1
Pillow==10.2.0

##FOR :

Flask → Web framework for backend

Flask-PyMongo → MongoDB integration with Flask

pymongo → MongoDB driver

Werkzeug → Password hashing & security

python-dotenv → Load environment variables (.env)

Pillow → Image upload, resize (gallery feature)